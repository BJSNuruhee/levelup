<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.UserMapper">
	
<!-- 	유저 테이블 전체 조회 -->
	<select id="getLists" resultType="com.example.demo.entity.User">
		select * from tb_user
	</select>
	
<!-- 	회원 가입 -->
	<insert id="postSignUp" parameterType="com.example.demo.entity.User">
		INSERT INTO tb_user
		(
		    USER_SEQ,
		    USER_ID,
		    USER_PW,
		    USER_EMAIL,
		    USER_PHONE,
		    USER_TOKEN
		)
		VALUES
		(
		    (SELECT nextseq('seqnumber') FROM DUAL),
		    #{userId},
		    #{userPw},
		    #{userEmail},
		    #{userPhone},
		    #{userToken}
		);
	</insert>
	
<!-- 	유저 id 목록 조회 -->
	<select id="getUserId" resultType="String">
		select user_id from tb_user
	</select>
	
	<select id="memLogin" parameterType="com.example.demo.entity.User" resultType="com.example.demo.entity.User">
		select * from tb_user
		where USER_ID = #{userId} and USER_PW = #{userPw}
	</select>
</mapper>